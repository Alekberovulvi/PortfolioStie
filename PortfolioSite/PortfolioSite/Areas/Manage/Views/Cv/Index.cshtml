@model HomeVM

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}

<h1>Cv</h1>

<br />


<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>FileName</th>
        <th>Delete</th>

    </tr>

    <tr>
        <th>@Model.Cvs.Id</th>
        <td>@Model.Cvs.Name</td>
        <td>@Model.Cvs.FileName</td>
        <td>
            <a asp-action="Delete" asp-route-id="@Model.Cvs.Id" class="btn btn-danger" onsubmit="return confirm('Are you sure you want to delete this CV?')><i class="fa-solid fa-trash"></i> Delete</a>
        </td>
    </tr>
</table>
<a asp-controller="Cv" asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Create</a>

<br />
<br />
<br />
<br />
<br />
<br />
<br />
<h1>CvIngilis</h1>


<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>FileName</th>
        <th>Delete</th>

    </tr>

    <tr>
        <th>@Model.CVmodel.Id</th>
        <td>@Model.CVmodel.Name</td>
        <td>@Model.CVmodel.FileName</td>
        <td>
            <a asp-action="Del" asp-route-id="@Model.CVmodel.Id" class="btn btn-danger" onsubmit="return confirm('Are you sure you want to delete this CV?')><i class="fa-solid fa-trash"></i> Delete</a>
        </td>
    </tr>
</table>
<a asp-controller="Cv" asp-action="Generate" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Create</a>




